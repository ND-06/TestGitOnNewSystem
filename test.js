const calculate = (a,b) => {
  return a * b;
}
console.log(calculate(10,9));

const sort = (arr) =>  {
  return arr.reverse();
}

const arrayNew = ["Miami", 'London', 'Paris'];

console.log(sort(arrayNew));